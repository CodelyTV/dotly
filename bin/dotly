#!/usr/bin/env bash

set -o pipefail

. "${SLOTH_PATH:-$DOTLY_PATH}/scripts/core/src/_main.sh"

##? Usage:
##?    dotly self-update
##?    dotly autoupdate
docs::parse "$@"

case $1 in
"self-update")
  "$DOTLY_PATH/bin/dot" self update
  ;;
*)
  exit 1
  ;;
esac
