- defaults:
    link:
      create: true
      force: true

- link:
    $DOTFILES_PATH/shell/init-scripts.enabled/marketplace: $DOTLY_PATH/shell/init-scripts/marketplace

- shell:
    - [echo ".marketplace-cache" >> $DOTFILES_PATH/.gitignore, Add marketplace cache to .gitignore]