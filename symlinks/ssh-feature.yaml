- clean: ['~/.ssh']

- create:
    - ~/.ssh/control
    - $DOTFILES_PATH/ssh/config.d
    - $DOTFILES_PATH/ssh/config.d-enabled

- link:
    ~/.ssh/config.d: $DOTFILES_PATH/ssh/config.d
    ~/.ssh/config.d-enabled: $DOTFILES_PATH/ssh/config.d-enabled
    ~/.ssh/config: $DOTFILES_PATH/ssh/config
