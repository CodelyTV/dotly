- defaults:
    link:
      create: true
      force: true
- link:
    shell/init-scripts.enabled/secrets:$DOTLY_PATH/shell/init-scripts/secrets_autload
    $DOTFILES_PATH:$DOTLY_SECRETS_REPOSITORY_PATH
# The idea is:
# Apply left part is applied in $DOTFILES_PATH and right part in the secrets repository

# TODO