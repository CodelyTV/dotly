- clean: ['~']

- defaults:
    link:
      create: true
      force: true

- create:
      - ~/.z

- link:
    ~/.zimrc: shell/zsh/.zimrc
    ~/.zlogin: shell/zsh/.zlogin
    ~/.zshenv: shell/zsh/.zshenv
    ~/.zshrc: shell/zsh/.zshrc
